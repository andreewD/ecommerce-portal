import styled from 'styled-components'

const Form = styled.form`
  margin: 30px auto 0;
  max-width: 400px;
`

const Title = styled.h1`
  font-weight: bold;
  margin: 0 0 10px;
`
export { Form, Title }
